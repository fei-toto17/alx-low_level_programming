#include <stdio.h>
/**
 * a program to print lowercase alphabets
 * return= zero(success) always
 */

int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++i)
	{
		putchar(ch);
	}
	putchar('/n');
return (0);
}
